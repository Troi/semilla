version: '3.8'
services:
  web:
    image: ghcr.io/getzola/zola:v0.19.2
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/site
    ports:
      - "1111:1111"
      - "1024:1024"
    command: serve --interface 0.0.0.0 --port 1111 --base-url localhost
